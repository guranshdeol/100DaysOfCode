// Define a variable called "name" and assign it a string value
var name = "John";

// Define a variable called "age" and assign it a number value
var age = 25;

// Define a function called "greet" that takes a name parameter and logs a greeting message to the console
function greet(name) {
    console.log("Hello, " + name + "!");
}

// Call the greet function and pass in the "name" variable as an argument
greet(name);

// Log the "age" variable to the console
console.log(age);
